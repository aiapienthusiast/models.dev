# Reasoning is always on and takes no caller control: reasoning_content comes back on every request, including
# with reasoning.enabled = false and with reasoning.effort = "none" (6 of 6 probed 2026-09-16).
# No image input on this host: an image_url part is rejected (400, or every upstream route failing) and the
# catalog reports capabilities.vision false (probed 2026-09-16).
# Rates: GET https://api.cheaperinference.com/v1/models (pricing_version sha256:1099de72…, checked 2026-09-16T08:00:58Z)
# Discounted marketplace rate, 30.00% off the model maker's list price ($2.5/$7.5 per 1M).
base_model = "alibaba/qwen3.8-max"
attachment = false
reasoning_options = []

[interleaved]
field = "reasoning_content"

[cost]
input = 1.75
output = 5.25
cache_read = 0.2125
cache_write = 2.125

[modalities]
input = ["text"]
output = ["text"]
