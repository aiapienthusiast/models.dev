# No image input on this host: an image_url part was rejected on two of three requests and the catalog reports
# capabilities.vision false, so image input is not dependable here (probed 2026-09-16).
# Rates: GET https://api.cheaperinference.com/v1/models (pricing_version sha256:1099de72…, checked 2026-09-16T08:00:58Z)
# Discounted marketplace rate, 30.00% off the model maker's list price ($5.0/$25.0 per 1M).
base_model = "anthropic/claude-opus-5"
attachment = false

[interleaved]
field = "reasoning_content"

[[reasoning_options]]
type = "effort"
values = ["low", "medium", "high", "xhigh", "max"]

[cost]
input = 3.5
output = 17.5
cache_read = 0.35
cache_write = 4.25

[modalities]
input = ["text"]
output = ["text"]
