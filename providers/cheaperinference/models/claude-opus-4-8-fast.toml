# Toggle: reasoning.enabled = true|false
# No image input on this host: an image_url part is rejected (400, or every upstream route failing) and the
# catalog reports capabilities.vision false (probed 2026-09-16).
# Rates: GET https://api.cheaperinference.com/v1/models (pricing_version sha256:1099de72…, checked 2026-09-16T08:00:58Z)
# Discounted marketplace rate, 30.00% off the model maker's list price ($12.0/$60.0 per 1M).
base_model = "anthropic/claude-opus-4-8"
attachment = false

[interleaved]
field = "reasoning_content"

[[reasoning_options]]
type = "toggle"

[[reasoning_options]]
type = "effort"
values = ["low", "medium", "high", "xhigh", "max"]

[cost]
input = 8.4
output = 42.0
cache_read = 0.84
cache_write = 10.2

[modalities]
input = ["text"]
output = ["text"]
