# Image input verified on this host: a 32x32 solid-colour PNG as a data: URI was named correctly on
# 4 of 4 colour probes (probed 2026-09-17).
# Reasoning is off unless the request asks for it, and the caller grades it. Across 34 requests the route
# that answers as anthropic/claude-opus-4-5 returned 0 reasoning tokens for plain requests (2 of 2),
# reasoning_effort = "none" (2 of 2) and reasoning.enabled = false (2 of 2); 32 tokens for "low"; 32-105
# for "medium" (4 of 4); 242-246 for "high" (2 of 2); and 276-313 for thinking.type = "enabled" with
# budget_tokens (3 of 3). That route reports the work in usage.completion_tokens_details.reasoning_tokens
# and returns no reasoning_content, so no interleaved field is declared. A second route answers as
# claude-opus-4-5 through Amazon Bedrock and returns 0 for every form, which is why a single request can
# miss the controls (probed 2026-09-17).
# Rates: GET https://api.cheaperinference.com/v1/models (pricing_version sha256:6d2520b9…, checked 2026-09-17T07:00:28Z)
# Discounted marketplace rate, 30.00% off the model maker's list price ($5.0/$25.0 per 1M).
base_model = "anthropic/claude-opus-4-5"

[[reasoning_options]]
type = "toggle"

[[reasoning_options]]
type = "effort"
values = ["none", "low", "medium", "high"]

[cost]
input = 3.5
output = 17.5
cache_read = 0.35
cache_write = 4.25

[limit]
context = 198000
output = 32768
